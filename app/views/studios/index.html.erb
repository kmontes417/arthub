  <div class="flex-container mx-auto" style="width: 95%;">
   <div class="row">
    <div class="col-8">
      <% @studios.each do |studio| %>
      <div class="card-trip">
        <img src="<%= studio.photo %>">
        <div class="card-trip-infos">
          <div>
            <h2><%= studio.name %></h2>
            <p><%= studio.description %></p>
          </div>
          <h2 class="card-trip-pricing">£ <%= studio.price %></h2>
        </div>
      </div>

     <% end %>


    </div>

    <div class="col-4"
    id="map"
    style="width: 75%;
    height: 600px;"
    data-markers="<%= @markers.to_json %>"
    data-mapbox-api-key="<%= ENV['MAPBOX_API_KEY'] %>"
  ></div>

  </div>

</div>





