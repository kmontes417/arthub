<%= simple_form_for [@studio, @booking] do |f| %>

  <%= f.input :checkin, as: :string, label:"Check-in & Check-out", label_html: { class:"check_in_date"}, input_html: { id: "range_start" } %>
  <%= f.submit "Book the studio", class: 'search-btn mb-3' %>
<% end %>

